# Springboot-User-Management-Client-Web-App
A frontend project which has the functionality to consume REST APIs and perform CRUD operations by hitting the rest end points. <br>
We can use a client to consume the rest api and perform user login and registration and also manage the users all in one place.
